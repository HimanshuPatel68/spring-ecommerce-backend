# 🛍️ E-Commerce Backend Application

This project is a simple yet powerful **E-Commerce Backend** built using **Spring Boot**, **Spring Data JPA**, and **PostgreSQL**.  
It provides RESTful APIs for managing products, orders, and order items — following a clean architecture with **Controller**, **Service**, and **Repository** layers.

---

## 🧾 Description

- Developed a RESTful backend using **Spring Boot** with a clear and modular structure (Controller, Service, Repository).
- Created database models for **Products**, **Orders**, and **Order Items**, connected via **Hibernate relationships**.
- Integrated with a **PostgreSQL** database using **Spring Data JPA**, enabling automatic table creation and updates.
- Implemented key features such as:
    - 🛒 Product search functionality
    - 🖼️ Image upload support
    - 💰 Automatic order total calculation
- Designed for easy integration with a React frontend or any REST client.

---

## 🌱 Simple Spring Boot Application

This backend is based on **Spring Boot**, which helps you start and configure applications with minimal setup.  
Spring Boot automatically configures dependencies based on your classpath, allowing faster and cleaner development.

---

## 📘 Overview
- Quick setup using Spring Boot
- Minimal configuration required
- Clean layered architecture (Controller → Service → Repository)
- Easily extendable for authentication and payment modules

---

## ⚙️ Key Topics
- Exception Handling (`@ExceptionHandler`, `@ControllerAdvice`)
- REST Controllers and Responses
- Beans and Configurations (`@Configuration`, `@Bean`, `@Value`)
- Components (`@Component`, `@Service`, `@Repository`)
- Dependency Injection (`@Autowired`, `@Qualifier`, `@Primary`)
- YAML Configuration and Interceptors

---

## 🧩 Tech Stack
- **Java 17+**
- **Spring Boot 3.x**
- **Spring Data JPA / Hibernate**
- **PostgreSQL Database**
- **Maven Build Tool**

---

## 🧾 Future Enhancements
- 🔐 Add user authentication (JWT + Spring Security)
- 💳 Integrate payment gateway
- 📦 Enable product categories and inventory tracking

---
